# Frontend

React frontend for the Mini Enterprise Collaboration Workflow project.

## Scripts

```bash
npm install
npm run dev
npm run lint
npm run build
```

The frontend uses React Router for protected pages, Axios for API calls, Tailwind CSS for styling, Recharts for dashboard charts, and `@dnd-kit` for Kanban drag/drop.
